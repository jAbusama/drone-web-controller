export enum TagTypes {
	DRONE_INSTRUCT = 'drone-instruct',
	GET_BILLBOARD = 'get-billboard',
}

export enum DroneCommandValues {
	UP = '^',
	DOWN = 'v',
	RIGHT = '>',
	LEFT = '<',
	CAMERA = 'x',
}
